# AGENTS.md

このファイルは AI エージェント（Claude Code 等）がこのリポジトリで作業する際のガイダンスを提供する。

## Project Overview

safe-rm は AI エージェント（Claude Code等）向けの安全なファイル削除プロキシ。常時プロジェクト境界と Git 管理メタデータ（`.git` 等）を保護し、`allow_project_deletion = false` の厳格モードでは未コミットのファイル削除もブロックする Rust CLI ツール。

## Commands

```bash
make build              # デバッグビルド
make release            # リリースビルド
make install            # /usr/local/bin にインストール
make test               # 全テスト実行
make test-unit          # ユニットテストのみ (cargo test --lib)
make test-integration   # 統合テストのみ (cargo test --test integration_test)
make fmt                # コードフォーマット
make check              # clippy (-D warnings) + cargo check

# 単一テスト実行
cargo test <test_name>
cargo test --test integration_test <test_name>
```

## Toolchain

- 最小サポート Rust バージョンは 1.87。`Cargo.toml` の `rust-version` と README の要件を同期する
- CI の `msrv` ジョブは Rust 1.87.0 で `cargo check --locked --all-targets --all-features` を実行する
- CI とリリースビルドはコミット済みの `Cargo.lock` を `--locked` で強制し、Release の lockfile 再生成エラーは握りつぶさない。Release は workflow_dispatch 対象の同一 `github.sha` を全 matrix でビルドし、すべて成功した後にだけリリースコミットとタグを push する

## Architecture

### 実行フロー

```
CLI引数パース → Config読込 → パス毎に処理 → `.`/`..` operand拒否 → allowed_paths判定 → [必要時 Git repo検出] → [Git管理メタデータ保護] → [必要時 Git status一括取得] → 削除/ブロック
```

### モジュール構成

| モジュール | 責務 |
|---|---|
| `main.rs` | エントリポイント。削除フロー全体のオーケストレーション、複数パスのバッチ処理。`process_path()` は最初に `PathChecker::reject_dot_or_dotdot_operand()` を呼び、末尾成分が `.` / `..` の operand を allowed_paths バイパスや `-f` より先に exit 2 で拒否する（POSIX rm は同じ operand を一切処理しないため、`safe-rm -r .` で cwd 自体、`safe-rm -r ..` で親ディレクトリが消える置き換え前より危険な挙動を塞ぐ）。`GitContext` で Git リポジトリ検出を allowed_paths 以外の安全境界が必要になるまで遅延し、壊れた cwd の `.git` が許可パス削除を巻き込まないようにする。allowed_paths 分岐と標準チェック分岐で完全に重複していた「`symlink_metadata()` 取得＋`-f` 時の NotFound スキップ＋ディレクトリの `-r` 必須判定」は `fetch_target_metadata()` に集約し、分岐ごとの安全チェック順序（allowed は Git メタデータ保護後、標準は包含検証＋Git メタデータ保護後）は維持したまま重複だけを排除する。`process_path()` は allowed_paths 判定の前に `symlink_metadata()` でディレクトリ性質を事前確認し（取得失敗は握りつぶし後続の `fetch_target_metadata()` に正規エラー経路を委ねる）、`Config::is_path_allowed_for_removal()` 経由で非再帰 allowed エントリ配下のディレクトリへの `-r` を allowed 分岐から外す。これにより allowed バイパスが効かなくなった経路は標準分岐の包含検証と Git ステータス検査に落ちる。strict mode（`allow_project_deletion = false`）の Git ステータスチェックは、cwd の `GitContext::checker()` だけに頼らず、対象起点でも `GitChecker::open()` を必ず試行し、対象を含む repo のうち最も深い workdir を持つ checker を選択する。これにより、(1) cwd が非 Git で配下の Git リポジトリ内のファイルを削除する場合に strict 判定が完全にスキップされて未コミット変更が素通りする不具合、(2) cwd の outer repo が nested repo を `.gitignore` で除外しているケースで outer 側の Ignored 判定により nested の modified/staged が見落とされる不具合、(3) cwd の Git と別の Git にある対象を強制モードで素通りさせる不具合を、いずれも fail-closed で塞ぐ。さらに、対象起点で discover した repo が `core.worktree` 等で論理ワークツリーを別ディレクトリへリダイレクトしていて、その `workdir()` が削除対象を含まない場合は、`to_workdir_relative()` が `None`→`NotInRepo`（削除可能）に落ちる fail-open を避けるため対象を `Modified` 扱いで fail-closed にブロックする（bare リポジトリ = workdir None は Git 管理メタデータ保護で別途守られるため対象外）。`status_cache` は単一の `HashMap` ではなく「最後に使った repo workdir をキー」とする `Option<(PathBuf, HashMap<Vec<u8>, FileStatus>)>` で保持し、同じ repo の連続削除ではキャッシュを再利用、異なる repo に切り替わったら入れ替える |
| `cli.rs` | clap derive による引数定義 (`-r`, `-f`, `-n`, `init` サブコマンド) |
| `config.rs` | `~/.config/safe-rm/config.toml` の読込。`allowed_paths` と `allow_project_deletion` の管理。`Path::exists()` で fail-open に倒れないよう `read_to_string()` の結果で分岐する。設定パス自体を決定できない `None` は「設定がない」と断定できないため fail-closed で strict モードへ倒す。`read_to_string()` が `ErrorKind::NotFound` を返した場合は、`has_dangling_symlink_component()` でパスのコンポーネントをルートから辿り、最終・中間いずれかが dangling symlink（リンク先を解決できない symlink）なら「設定は置かれているが読めない」状態とみなして fail-closed で strict モードへ倒す（`read_to_string()`・`symlink_metadata(&path)` ともに中間 dangling symlink ではパス全体で `NotFound` になり真の不在と区別できないため、コンポーネント単位で判定する）。純粋にファイルが未作成なだけなら従来どおり permissive default を返し、解決可能な symlink 配下の未作成 config も permissive を維持して過剰 strict 化しない。その他の I/O エラー（権限不足等）と TOML パース失敗は fail-closed で `allow_project_deletion = false`（strict モード）にフォールバックする（`fail_closed_default()`）。利用者が意図した strict 設定が不明な設定位置、壊れた構文、stat 失敗、symlink 切れだけで無効化されない。`is_path_allowed()` は削除対象エントリ位置（`canonicalize_parent_keep_filename()` で親のみ canonicalize し末尾 symlink は保持）と末尾まで解決した実体位置の両方が許可エントリ内であることを要求し（`path_matches_allowed_entry()`）、許可ディレクトリ外の symlink が許可内を指すケース（エントリが外）の削除を防ぐ。`is_path_allowed_for_removal(target, target_is_dir, recursive_delete)` は削除操作の意味論まで含めて判定し、`target_is_dir && recursive_delete` 指定時は非再帰の allowed エントリを候補から除外する。これにより `recursive = false` エントリ配下の直接の子ディレクトリへの `-r` が、直接の子の範囲を超えた配下の再帰削除を allowed バイパスで実行する不具合を fail-closed で塞ぐ（`is_path_allowed()` は後方互換のため `is_path_allowed_for_removal(target, false, false)` の thin wrapper として残す） |
| `error.rs` | `SafeRmError` enum（終了コード: 0=成功, 1=操作エラー, 2=セキュリティブロック）、`FileStatus` enum（`is_deletable()` メソッド付き）。`PartialFailure` には `dry_run` フラグがあり、ドライラン時は「would be removed」と表示する。解決不能な中間 symlink 配下の削除は `DanglingIntermediateSymlink` として exit 2 でブロックする。末尾成分が `.` / `..` の operand は `DotOrDotDotOperand` として exit 2 でブロックする |
| `path_checker.rs` | パス正規化、プロジェクトルート内包含検証、シンボリックリンク解決、非存在パスでも既存親を canonicalize して別名パス差異を吸収、ディレクトリトラバーサル防止。`reject_symlink_parent_traversal()` は `..` の直前成分が「実体として存在する通常ディレクトリ」であることを必須とし、symlink/通常ファイル/特殊ファイル/存在しない/メタデータ取得失敗の場合は `UnsafeTraversal` で fail-closed に拒否する（`link/../victim`・`missing/../victim`・`file/../victim` の正規化すり替え対策）。`reject_dangling_intermediate_symlink()` は `dangling/child.txt` のように中間コンポーネントが解決不能な symlink のパスを削除前に fail-closed で拒否する一方、末尾の dangling symlink 自身はリンクエントリだけを削除するため許可する。`reject_dot_or_dotdot_operand()` は末尾成分（末尾セパレータを除いた最後の要素）が `.` / `..` の operand を `DotOrDotDotOperand` で拒否する。判定に使う `last_component_is_dot_or_dotdot()` は `Path::components()` を使わない — `components()` は `foo/.` の末尾 `.` を字句正規化で落として `Normal("foo")` にしてしまい、basename が `.` の operand を検出できないため、生の `OsStr` から末尾成分を切り出して比較する（`.` / `..` は ASCII なので非 UTF-8 パスでも `to_string_lossy()` の置換文字と取り違えない）。`.hidden` や `...` のような dot で始まる通常のファイル名は従来どおり削除できる。Windows の drive-relative 形式（`C:.` / `C:..`）はセパレータを含まず末尾成分の比較に掛からないため、`is_drive_relative_dot_component()` で追加判定する（Unix では `C:.` が正当なファイル名になり得るので拒否は Windows 限定、判定ロジック自体は全プラットフォームでユニットテストする）。`verify_containment_with_base()` は削除対象エントリ（末尾コンポーネント、symlink を辿らない位置。`canonicalize_parent_keep_filename()` で親のみ canonicalize し末尾は保持）と末尾まで解決した実体位置の両方がプロジェクト内であることを要求し、プロジェクト外の symlink がプロジェクト内を指すケース（エントリが外）と、プロジェクト内の symlink がプロジェクト外を指すケース（実体が外）の双方をブロックする |
| `git_checker.rs` | Git リポジトリ検出、ファイルステータス判定 (Clean/Modified/Staged/Untracked/Ignored/NotInRepo)、ディレクトリ再帰チェック（symlink非追従）、Git 管理メタデータを含む再帰削除の検出。`GitChecker::open()` は `Result<Option<Self>, SafeRmError>` を返し、Git API エラー（壊れた `.git`、権限不足、I/O エラー等）は fail-closed で `Err` を伝播する。`Repository::discover()` が `NotFound` を返した場合でも `has_git_metadata_ancestor()` で `path` の祖先に `.git` 痕跡が残っていないか確認し、痕跡があれば「壊れたリポジトリ」とみなして `Err` に倒す。祖先確認そのものが権限/I/O エラーで信頼できない場合も fail-closed とし、`.git` 痕跡がなく確認にも成功した場合のみ非 Git 環境として `Ok(None)` を返す。`path_targets_or_contains_git_metadata()` / `try_path_targets_or_contains_git_metadata()` で削除対象自体・任意の中間コンポーネント・再帰削除時の配下に含まれる任意階層の Git 管理メタデータを検出し、ネストしたリポジトリのメタデータも保護する。`.git` ファイル/ディレクトリに加え、`.git` コンポーネントを持たない bare リポジトリのルートや配下の管理ファイルも検出する。bare リポジトリ判定 `is_bare_repository_root()` は `Repository::open_bare()` の成否だけに依存せず、`has_bare_repository_markers()` で構造マーカー（`HEAD` エントリ・`objects/`・`refs/`）を確認し、`config` 破損で `open_bare()` が失敗する bare リポジトリも fail-closed で検出する（`HEAD` は古い `core.preferSymlinkRefs` の symlink でリンク先 ref が未作成でも `symlink_metadata()` で存在のみを確認）。中間 symlink 経由のバイパスを防ぐため、親ディレクトリのみ canonicalize して末尾コンポーネントは保持する `canonicalize_parent_keep_filename()` を併用する（symlink 自身の削除は許可しつつ、`gitlink/config` のように中間 symlink で `.git` を指すケースは確実にブロックする）。`touches_git_metadata_path()` と `is_git_metadata_path()` も同じ `canonicalize_parent_keep_filename()` を使い、`.git` を指す symlink 自身の削除（リンクのみが消え実体は残る）を許可する一方で、symlink 経由で `.git` 配下に到達するパスは引き続きブロックする。コンポーネント比較は ASCII case-insensitive で行い、macOS APFS のような大文字小文字を区別しないファイルシステムで `.GIT` 経由のバイパスも防ぐ。`convert_status()` は `Status::CONFLICTED` を Modified にマッピングして、CONFLICTED が単独で立つ稀なケースでも Clean に落として削除許可してしまう不具合を防ぐ。さらに `Status::WT_UNREADABLE`（libgit2 が作業ツリーのファイルを読み取れない状態）も Modified にマッピングし、fallback は「空ビット（`CURRENT`）のときだけ Clean、それ以外は Modified」とすることで、未処理のフラグ（将来 git2 に追加されるフラグ含む）を fail-closed で削除ブロックする。実削除前の検査ではディレクトリ読み取り・エントリ取得エラーを `DirectoryReadError` として返し、fail-closed でブロックする。ワークディレクトリは構造体に canonicalize 済みでキャッシュし、`to_workdir_relative()` で canonical/未解決両方のパスに対応。`status_file()` が未追跡ディレクトリを畳み込むケースでは、再帰付き status 一覧で再確認してネストした未追跡ファイルを取りこぼさない。ディレクトリ配下は常に再帰検査し、ignored ファイルは許可する一方で、ignored 親ディレクトリ配下の tracked 変更済み/ステージング済みファイルや未追跡ファイルはブロックする。ディレクトリの削除検査（`check_path()` / `check_path_with_cache()`）はまず read_dir ベースの再帰検査でディスク上のファイルを個別に確認し（ダーティなファイルは具体パスでブロック）、その後 `check_cached_statuses_under_directory()` でステータスキャッシュを最上位エントリで 1 回だけ prefix 走査して、`git rm` 済み（`INDEX_DELETED`→Staged）や worktree から消えた（`WT_DELETED`→Modified）ファイルのように read_dir には現れないがステータスには載る「未コミットの削除」を取りこぼさずブロックする。`include_unmodified` で配下全ファイルがキャッシュに載るため最上位 1 回の走査で任意の深さを網羅でき、再帰のたびに再走査して O(深さ×件数) に劣化することも避ける。prefix 一致は `dir_key + '/'` 境界（および空 `dir_key`=ルート時の全件）で判定し、`dir`/`dir2` のような prefix 衝突で別ディレクトリを巻き込まない。Git API エラー時は fail-closed で削除をブロック（`get_all_statuses` は `Result` を返し、`resolve_status_from_relative_path` は予期しないエラーで `Modified` を返し、`lookup_status_in_listing` も `statuses()` 失敗時に `Modified` を返す）。ステータスキャッシュキーは `HashMap<Vec<u8>, FileStatus>` のバイト列で持ち、git2 0.21 で `Result<&str, git2::Error>` を返すようになった `StatusEntry::path()` の UTF-8 制限を避けて、`entry.path_bytes()` を直接キーに使う。`to_git_relative_key()` も `Vec<u8>` を返す（Unix では `OsStrExt::as_bytes()`、それ以外は `to_string_lossy()` フォールバック）。これにより非 UTF-8 ファイル名でもキャッシュに正しく登録され、`NotInRepo` に落ちて削除許可される fail-open 経路を塞ぐ。キャッシュミス時は、まず `resolve_status_from_relative_path()`（`status_file()` ベース）で判定し、`NotInRepo` のときだけ `ignored_status_from_relative_path()` の `.gitignore` 判定で補完する。ignore 判定を先に評価すると、`status_should_ignore()` が tracked 状態を考慮しないため、`git add -f` で追跡下に入れた `.gitignore` 一致の tracked+dirty ファイルを Ignored（削除可能）と誤判定する fail-open が生じる。これを status_file 優先の順序で塞ぐ。`ignored_status_from_relative_path()` は `Ok(true)→Ignored`/`Ok(false)→None`/`Err→Modified` に分岐し、`status_should_ignore()` の Git API エラーも fail-closed で削除をブロックする。`lookup_status_in_listing()` 末尾の `status_should_ignore()` 判定も同じく Err を `Modified` に倒す |
| `init.rs` | `safe-rm init` によるデフォルト設定ファイル生成（`~/.claude/skills` と `/tmp` を再帰許可）。設定ファイルの既存確認は `symlink_metadata()` で dangling symlink も既存エントリとして扱い、`OpenOptions::create_new(true)` で race 時も既存ファイルを上書きしない。これにより `Path::exists()` が dangling symlink を `false` と返して `fs::write()` がリンク先へ新規設定を書き込む経路を防ぐ |

`GitChecker::open()` が保持する workdir は `canonicalize_workdir()` で必ず実体パスへ解決し、canonicalize 失敗を未解決パスへフォールバックせず `SafeRmError::IoError` として伝播する。ディレクトリ判定用の `is_real_directory()` も `Result<bool, SafeRmError>` を返し、`symlink_metadata()` の失敗を「通常ファイル」と誤認して単一ファイルの Git 判定へ流さず、再帰検査開始前に fail-closed でブロックする。

`main.rs` の strict Git 判定は `check_strict_git_status()` に分離し、対象 repo の選択を `select_target_checker()`、`core.worktree` リダイレクト時の包含確認を `ensure_discovered_workdir_contains_target()`、repo 別 status キャッシュの更新と検査を `check_path_with_status_cache()` が担当する。`process_path()` は安全チェック順序のオーケストレーションに専念する。`select_target_checker()` は outer/nested repo のうち対象を含む最も深い workdir を優先し、cwd repo が対象を含まない場合に選ばれないことをユニットテストで直接検証する。symlink の親ディレクトリ解決に失敗した場合は未解決パスへフォールバックせず、I/O エラーを伝播して fail-closed にブロックする。

### セキュリティモデル

1. **Git管理メタデータ保護** (常時有効): `.git`、gitdir 参照ファイル、bare リポジトリ管理パス、現在のリポジトリの Git 管理メタデータを含む再帰削除に加え、削除対象自身・パスの任意の中間コンポーネント・再帰削除時に配下に存在する任意階層の Git 管理メタデータ（ネストした `.git` と bare リポジトリを含む）をブロック。bare リポジトリは `config` 破損で `Repository::open_bare()` が失敗しても構造マーカー（`HEAD`/`objects/`/`refs/`）で検出して保護する。コンポーネント比較は ASCII case-insensitive で、macOS APFS のような大文字小文字を区別しないファイルシステムでの `.GIT` 経由バイパスも防止
2. **パス包含検証** (常時有効): `allowed_paths` 以外では、再帰的な Git 管理メタデータ探索より先にプロジェクトルート外への削除をブロック。削除対象エントリ（末尾 symlink を辿らない位置）と末尾まで解決した実体位置の両方がプロジェクト内であることを要求し、プロジェクト外の symlink がプロジェクト内を指すケースも、プロジェクト内の symlink がプロジェクト外を指すケースもブロックする
3. **Git保護** (`allow_project_deletion = false` 時): Modified/Staged/Untracked/コンフリクト中ファイルの削除をブロック（CONFLICTED フラグも Modified として扱う）。`WT_UNREADABLE`（作業ツリーのファイルを読めない状態）や未処理フラグも fail-closed で Modified 扱いとし、`convert_status()` の fallback は空ビット（`CURRENT`）のときだけ Clean を返す。ディレクトリの再帰削除では、read_dir に現れない削除済みエントリ（`git rm` 済み=Staged・worktree から削除=Modified）も `check_cached_statuses_under_directory()` のステータスキャッシュ走査でブロックし、配下に未コミットの削除を含むディレクトリの削除を許さない。判定に使う Git リポジトリは cwd 由来の checker だけでなく、削除対象起点でも `GitChecker::open()` を試行し、対象を含む repo のうち最も深い workdir を持つ checker を採用する。これにより cwd 非 Git で対象側だけ Git のケース、cwd の outer repo が nested repo を `.gitignore` で除外しているケース、cwd と別 Git の対象を指定するケースのいずれでも対象側 repo の status で判定する。加えて、対象を含む repo が `core.worktree` でワークツリーをリダイレクトしていて workdir が対象を含まない場合は、`Modified` 扱いで fail-closed にブロックし `NotInRepo` 素通りを防ぐ
4. **allowed_paths**: 設定ファイルで指定したパスは包含チェックと Git ステータスチェックをバイパスするが、Git管理メタデータ保護はバイパスしない。許可判定は削除対象エントリ位置と実体位置の両方が許可エントリ内であることを要求し、許可ディレクトリ外の symlink が許可内を指すケースの削除を防ぐ。`recursive = false` エントリは「直接の子のみ」を許可し、その配下のディレクトリへの `-r` 付き削除は `is_path_allowed_for_removal()` で非再帰エントリを候補から除外することで allowed バイパスを許さない（標準分岐に落ちて包含検証と Git ステータス検査の対象になる）。現在のリポジトリ情報は可能なら使うが、cwd の `.git` が壊れていても許可パス削除は Git 検出失敗に巻き込まれない
5. **Fail-Closed**: ディレクトリ読取エラー、`allowed_paths` 外の通常経路で必要になる `Repository::discover()` 由来の Git API エラー（壊れた `.git`/権限不足/I/O 等。`NotFound` でも祖先に `.git` 痕跡が残っている、または祖先確認が権限/I/O エラーで完了できない場合は `has_git_metadata_ancestor()` が fail-closed に倒す）、解決不能な中間 symlink、設定ファイル読込/パースエラー時はすべて fail-closed。Git API エラーは `SafeRmError::GitError` として伝播し、設定ファイルが読み取れない/パースできない場合は `Config::fail_closed_default()`（`allow_project_deletion = false`）にフォールバックして利用者の strict 意図を守る。Config の存在判定は `Path::exists()` ではなく `read_to_string()` の `ErrorKind::NotFound` で行い、stat 失敗で fail-open に倒れない。`read_to_string()` が `NotFound` を返した場合は `has_dangling_symlink_component()` でパスのコンポーネントを辿り、最終・中間いずれかが dangling symlink なら「設定はあるが読めない」とみなして strict モードへ倒す（中間 dangling symlink は `symlink_metadata(&path)` でも真の不在と区別できないため）。純粋な未作成や、解決可能な symlink 配下の未作成 config は従来どおり permissive default を維持する。`safe-rm init` の生成先も `symlink_metadata()` と `create_new` で既存エントリを保護し、dangling symlink のリンク先へテンプレートを書き込まない。`status_should_ignore()` の `Err` も `ignored_status_from_relative_path()` と `lookup_status_in_listing()` で `Modified` に倒し、Git API エラーを握りつぶして偶発的に削除許可されないようにする
6. **Ignored混在ディレクトリ保護**: 厳格モードでは ignored ディレクトリでも配下を再帰検査し、ignored ファイルは許可する一方で tracked 変更済み/ステージング済みファイルや未追跡ファイルをブロックする
7. **Symlink安全性**: Gitチェック時のディレクトリ判定および任意階層 `.git` 検出は `symlink_metadata()` ベースで、ディレクトリsymlinkを辿らずリンク自体を評価。削除対象パスの中間コンポーネントが dangling symlink の場合は、後からリンク先が実体化されて境界外削除に化ける経路を避けるため `DanglingIntermediateSymlink` として fail-closed に拒否する。末尾の dangling symlink 自身はリンクエントリだけを削除するため従来どおり許可する
8. **`..` 解決による別ファイルすり替え防止**: `..` の直前成分が「実体として存在する通常ディレクトリ」でないパス（symlink、通常ファイル、特殊ファイル、存在しない/読み取り不能な中間成分等）は削除前に拒否する。OS の path resolution と `path_clean` の字句正規化の差を利用して、`link/../victim`・`missing/../victim`・`file/../victim` のように本来到達できない別ファイルを削除する経路をブロックする。メタデータ取得・削除・許可判定は引き続き `path_clean` で字句的に `..` を解決した正規化パス（`normalized_path`）で行う
9. **エイリアスパス耐性**: パス包含検証と `allowed_paths` 判定では、非存在パスでも既存親ディレクトリまで canonicalize して未作成部分を再結合し、repo symlink 別名や `/var` と `/private/var` 差異を吸収。Git管理メタデータ保護と Gitチェックでは非symlinkパスを canonicalize して比較し、symlink パスは「親ディレクトリのみ canonicalize + リンク名維持」で照合することでバイパスを防止（repo symlink 別名を cwd にした場合も含む）
10. **中間 symlink 経由 `.git` バイパス防止**: `gitlink -> nested/.git` のような symlink を中間に挟んで `gitlink/config` を削除しようとしても、親ディレクトリを canonicalize して `.git` コンポーネントの存在を確認することで実体パスへの到達をブロックする。bare リポジトリへの中間 symlink 経由削除も同様に防止。symlink 自身の削除はリンクのみが消えて実体は残るため、リンク先が `.git` や bare repo であっても削除を許可する（過剰防御を回避）。`touches_git_metadata_path()` と `is_git_metadata_path()` も同じ方針で symlink 自身を許可し、現在のリポジトリの `.git` を指す symlink でも一貫した挙動になる
11. **非UTF-8パスの fail-closed**: `StatusEntry::path()` の UTF-8 制限を避け、キャッシュキーをバイト列 (`HashMap<Vec<u8>, FileStatus>`) で持つ。`get_all_statuses()` は `entry.path_bytes().to_vec()` でキーを生成し、`lookup_status_in_listing()` も `entry.path_bytes()` で比較する。非 UTF-8 ファイル名でもキャッシュに正しく登録され、`NotInRepo` （削除許可）に落ちる fail-open 経路が塞がれる。キャッシュミス時の ignore 判定も `ignored_status_from_relative_path()` で `Err→Modified` に倒し、`status_should_ignore()` の Git API エラーを握りつぶす経路を排除
12. **Symlink エントリ位置での境界・許可判定**: 包含検証 `verify_containment_with_base()` と allowed_paths 判定 `is_path_allowed()` は、削除対象エントリ（末尾コンポーネント、symlink は辿らない位置。中間 symlink/エイリアスは解決）と、末尾まで解決した実体位置の両方が境界内/許可内であることを要求する。`remove_file`/`remove_dir` は symlink を辿らずリンクエントリ自体を削除するため、「実際に削除されるエントリ」がプロジェクト外/許可外であれば、そのリンク先がプロジェクト内/許可内でもブロックする（実体だけ見て境界を通過させてしまう包含バイパスの防止）。逆にプロジェクト内/許可内の symlink がプロジェクト外を指す場合は実体位置チェックで従来どおりブロックされる
13. **`.` / `..` operand の拒否** (常時有効): 末尾成分が `.` または `..` の operand（`.`・`./`・`..`・`../`・`sub/.`・`sub/..`・`/abs/path/..` 等）は `DotOrDotDotOperand` として exit 2 でブロックする。POSIX の rm は operand の basename が dot / dot-dot のとき診断メッセージを出して**その operand を一切処理しない**（GNU coreutils も `refusing to remove '.' or '..' directory` として skip する）。この拒否がないと `safe-rm -r .` がカレントディレクトリ自体を、`safe-rm -r ..` が親ディレクトリを実際に削除してしまい、置き換え対象である rm より危険側へ倒れる。判定は `process_path()` の冒頭、正規化・allowed_paths 判定・`-f` 処理より前に行うため、allowed_paths のバイパスでも `-f` でも回避できない。削除したい対象はディレクトリ名で明示する（`safe-rm -r sub` / 親ディレクトリへ移動してから名前指定）。`.hidden` や `...`、`..foo` のような dot で始まる/終わる通常のファイル名は従来どおり削除できる。Windows では drive-relative 形式（`C:.` / `C:..`）も拒否する

### パフォーマンス最適化

- Git リポジトリ検出は allowed_paths 外の包含検証や strict Git チェックが必要になった時だけ必須化する。allowed_paths では可能な範囲で現在リポジトリの Git 管理メタデータ保護に使うが、Git 検出失敗だけでは許可パス削除をブロックしない
- `allow_project_deletion = false` かつ `allowed_paths` 外の削除が必要になった時だけ Git status を一括取得（バッチ最適化、Clean ファイルもキャッシュに含め、allowed_paths は Git status エラーに巻き込まない）。キャッシュは「最後に使った repo workdir」をキーに `Option<(PathBuf, HashMap<...>)>` で 1 つだけ保持し、同じ repo の連続削除で再利用、異なる repo に切り替わったら入れ替える（複数 repo を交互に処理する場合は再ヒットしないが、これは正しさ優先のための妥協）
- `status_file()` の単体問い合わせで未追跡ディレクトリ配下を見落とすケースは、再帰付き status 一覧の再確認で補完
- Config の `allowed_paths` はロード時に既存親までパスを事前解決（canonicalize）
- `symlink_metadata()` で1回のsyscallで存在確認とメタ情報取得を統合
- Git ワークディレクトリは `open()` 時に1回 canonicalize してキャッシュ（毎回の syscall を回避）

### テスト構成

- **ユニットテスト**: 各モジュール内の `#[cfg(test)]` ブロック（パス検証、Git状態、Config解析、`SAFE_RM_CONFIG` の非 UTF-8 パス対応、`safe-rm init` が既存ファイルや dangling symlink を上書きせずリンク先も作成しないこと、symlink削除、dangling 中間 symlink のブロックと末尾 dangling symlink 自身の削除許可、symlink 経由の `..` 親ディレクトリ参照拒否、`reject_symlink_parent_traversal` による「`..` 直前成分が存在しない/通常ファイルである」パスの拒否、`link/child/../../victim` のように symlink 配下へ一度下ってから symlink 成分自体を消すパスの拒否、I/Oエラー、Git API エラー時の fail-closed 検証、壊れた `.git` や読み取り不可の探索対象で `GitChecker::open()` が `Err(GitError)` を返し permissive 経路へ抜けないこと、祖先に `.git` 痕跡がなく確認にも成功した場合のみ `Ok(None)` を返すこと、Config 読込/パースエラー時の strict モードフォールバック検証、Git管理メタデータ判定（`.git` を指す symlink 自身の削除は許可、symlink 経由のアクセスはブロックする挙動を `touches_git_metadata_path` / `is_git_metadata_path` 両方で検証）、Git管理メタデータを含む再帰削除の判定、`path_targets_or_contains_git_metadata` による任意階層 Git 管理メタデータ検出（ファイル/ディレクトリ自体・配下・深いネスト・`.GIT` 大文字バリアント・bare リポジトリ・`config` 破損で `open_bare()` が失敗する bare リポジトリの構造マーカー検出（`HEAD` symlink + 未作成 ref のケースを含む）・symlink 非追従・存在しないパス・再帰探索時のディレクトリ読み取りエラーを含む）、空リポジトリ対応、壊れた symlink 検出、複数ステータスの一括取得検証、キャッシュ使用時の ignored サブディレクトリチェック、ignored/未追跡混在ディレクトリのブロック、ignored ディレクトリ配下の tracked 変更済みファイルのブロック、`PartialFailure { dry_run: true }` のドライラン文言検証、`ensure_git_metadata_not_targeted()` 単体での `.git`/`.GIT` 中間コンポーネント検出と通常パスの許可検証、`git_checker = Some(&checker)` を渡したときに現在の repo の `.git` を含む再帰削除がブロックされ通常パスは許可されること、`fetch_target_metadata()` 単体での存在ファイル・`-f` 指定時の NotFound スキップ（`Ok(None)`）・`-f` 未指定時の NotFound・`-r` 未指定ディレクトリの IsDirectory・`-r` 指定ディレクトリ・ディレクトリへの symlink が `-r` 不要でメタデータを返すことの検証、非 UTF-8 ファイル名がバイト列キーで `get_all_statuses()` キャッシュに登録されること（macOS APFS 等で非 UTF-8 名作成不可な FS では `fs::write` 失敗で安全にスキップ）、非 UTF-8 ネスト未追跡ファイルが `check_file_with_cache` と空キャッシュ経由 `get_file_status_from_cache` の両方で削除不可（`Untracked`/`Modified`）と判定される fail-closed 検証、プロジェクト外の symlink がプロジェクト内の実体を指す場合に `verify_containment` がエントリ位置チェックでブロックすること、許可ディレクトリ外の symlink が許可内の実体を指す場合に `is_path_allowed` が拒否すること、`is_path_allowed_for_removal()` が `recursive = false` エントリ配下のディレクトリへの `-r` を拒否し（target_is_dir + recursive_delete のみで分岐）、通常ファイル判定や `-r` なしディレクトリ判定では従来通り許可すること、通常ファイルに `-r` を併用した場合も `target_is_dir = false` のため非再帰エントリでも許可されること、複数エントリ重複時に再帰エントリ経由で `-r` が許可されること、`is_path_allowed()` と `is_path_allowed_for_removal(target, false, false)` が等価であること、`path_matches_allowed_entry()` の単体検証（再帰エントリはエントリ自身のルートにマッチする一方、非再帰エントリはルート自身にも孫以下のパスにもマッチしないこと）、`convert_status()` が `WT_UNREADABLE` と未知フラグを `Modified` に倒し空ビットのみ `Clean` を返すこと、`check_path_with_cache()` が配下の staged deletion（`Staged`）/worktree deletion（`Modified`）をブロックし prefix が重なる別ディレクトリ（`dir` vs `dir2`）の削除では誤ブロックしないこと、Config が最終・中間コンポーネントの dangling symlink 設定パスで strict モードへフォールバックする一方、解決可能な symlink 配下の未作成 config では permissive default を維持すること、`get_file_status_from_cache()` がキャッシュミス時に `git add -f` された `.gitignore` 一致の tracked+dirty ファイルを（ignore 判定より status_file 判定を優先して）`Modified` としてブロックし、純粋な ignored 未追跡ファイルは従来どおり `Ignored`（削除可能）を維持すること、許可ディレクトリ内の symlink が許可範囲外を指す場合に `is_path_allowed()` が拒否すること、`reject_dot_or_dotdot_operand()` が `.`/`./`/`..`/`../`/`sub/.`/`sub/..`/`sub/./`/`sub/../`/`/abs/sub/.`/`/abs/sub/..` を拒否し `sub`/`sub/deep.txt`/`./sub/deep.txt`/`../sibling/file.txt`/`/`/`.git`/`.hidden`/`...`/`..foo`/`foo..` を許可すること、`DotOrDotDotOperand` の exit code が 2 でエラーメッセージが生の入力パスを保持すること、`is_drive_relative_dot_component()` が `C:.`/`C:..`/`z:.`/`z:..` だけを検出し `C:`/`C:foo`/`C:...`/`1:.`/`:.`/`Cx.`/`.`/`..` を検出しないこと、`select_target_checker()` が cwd checker なしで discover 結果を採用し・cwd repo が対象を含まない場合に別 repo の checker を選び・両方 None なら None を返すこと、`ensure_discovered_workdir_contains_target()` が discover 結果なし/workdir が対象を含む場合は素通りし・含まない場合は `Modified` でブロックすること、`check_path_with_status_cache()` が repo 切り替え時にキャッシュキーを入れ替えること、Config の設定パスの中間成分が通常ファイル（`NotADirectory`）のとき strict へフォールバックし・中間ディレクトリごと未作成なだけなら permissive default を維持すること等）
- **統合テスト**: `tests/integration_test.rs` - 実際のGitリポジトリを tempfile で作成してE2Eテスト。通常の CLI 実行ヘルパーは `SAFE_RM_CONFIG` に未存在パスを明示して、テスト実行ユーザーの `~/.config/safe-rm/config.toml` による環境依存を避ける。repo symlink 別名の cwd からの相対実行、単一失敗時の stderr 非重複、CLI ヘルプがデフォルトモードと厳格モードを誤説明しないこと、force フラグとダーティファイルの複合ケース、ネスト未追跡ディレクトリのブロック、ignored/未追跡混在ディレクトリのブロック、ignored ディレクトリ配下の tracked 変更済みファイルのブロック、プロジェクト外パスで包含検証を優先すること、ドライラン+フォース複合、空ディレクトリ処理、バッチセキュリティエラー優先、設定の複合テスト（strict mode + allowed_paths、複数 allowed_paths エントリ、Git status エラー時と cwd の Git 検出失敗時の allowed_paths バイパス）、strict mode + force フラグの複合テスト、相対パスの `..` コンポーネント検証、バッチ全ダーティの終了コード検証、allowed_paths ディレクトリ自体の削除挙動検証、allowed_paths でも現在のリポジトリの Git 管理メタデータ削除をバイパスできないこと、allowed_paths 配下でも中間 symlink 経由の `.git` 配下削除をブロックし、`.git` を指す symlink 自身の削除は許可すること、2パスバッチの終了コード優先度検証、symlink-to-directory の非再帰削除（末尾スラッシュ付きでもリンク先を残してリンクだけ削除することを含む）、Git index 破損時の fail-closed 検証、Config 設定ファイルが読めない/壊れたときに permissive default ではなく strict モードにフォールバックして未追跡削除がブロックされること、Git管理メタデータの常時ブロック、Git管理メタデータを含むリポジトリルート再帰削除のブロック、3パスバッチの終了コード優先度検証、ドライランのファイルシステム非変更保証、設定ファイルのエッジケース（空 allowed_paths 配列、存在しない allowed_paths ディレクトリ）、壊れた symlink のデフォルト/厳格モード対応、空リポジトリ厳格モード、バッチ force フラグ複合、allowed_paths ドライラン注釈、`link/../victim` 形式の symlink 経由 `..` 脱出ブロックと正規化後の別ファイルを削除しないこと（`-f` 併用も含む）、dangling 中間 symlink 配下の削除が exit 2 でブロックされること、live 中間 symlink がプロジェクト外を指す場合に包含検証でブロックされること、`missing_dir/../victim.txt` 形式（存在しない中間成分）の正規化すり替え拒否、`regular_file/../victim.txt` 形式（通常ファイル中間成分）の正規化すり替え拒否、`-f` 併用時も中間成分が通常ディレクトリでない `..` を fail-closed で拒否すること、非 Git 親直下リポジトリの `.git` 直接削除ブロック、ネストリポジトリ全体の再帰削除と内部 `.git` 直接削除のブロック、bare リポジトリ全体と内部管理ファイル削除のブロック、`config` 破損で `Repository::open_bare()` が失敗する bare リポジトリでも HEAD 削除と全体再帰削除をブロックすること、`nested/.git/config` 形式の中間コンポーネント `.git` 経由メタデータ削除ブロック、`nested/.GIT/config` のような大文字バリアント（macOS APFS 等の case-insensitive FS 対策）のブロック、`.GIT` を含むディレクトリの再帰削除ブロック、ドライラン部分失敗の「would be removed」表記検証、プロジェクト外の symlink がプロジェクト内のコミット済みファイルを指す場合に exit 2 でブロックし symlink 実体を残すこと（包含バイパス回帰防止）、`recursive = false` 配下の subdir への `-r` が allowed 分岐から外れて標準分岐の包含検証 / strict Git status 検査に落ちること（非 Git 親で包含検証拒否、strict mode で untracked による exit 2 ブロック、直接の子の通常ファイル削除は引き続き allowed バイパスで成功すること）、cwd 非 Git 親直下に Git リポジトリがあるケースで strict mode が対象側 repo の status で modified/staged/untracked を検出してブロックすること、同じ条件で対象側 repo の Clean ファイルは引き続き削除できること、cwd と削除対象が別 Git リポジトリの組み合わせでも対象側 status で modified をブロックできること、cwd 非 Git で複数 repo にまたがるバッチ削除でも各 repo の status で正しく判定されること、cwd の outer repo が nested repo を `.gitignore` で除外している環境でも nested 側 status で modified を検出してブロックすること（outer の Ignored 判定で素通りしないこと）、strict mode で配下に staged deletion / worktree deletion を含むディレクトリの `-r` 削除をブロックすること（別ディレクトリの staged deletion では clean ディレクトリの削除を阻害しないこと）、設定パスが最終・中間コンポーネントの dangling symlink のとき strict モードへフォールバックして未追跡削除をブロックすること、`git add -f` で追跡下に入れた `.gitignore` 一致の tracked+dirty ファイルが実削除フロー（get_all_statuses キャッシュ経由）でも Modified としてブロックされること、`core.worktree` でワークツリーをリダイレクトした repo のダーティ/クリーン両方の tracked ファイルが fail-closed でブロックされること、allowed_paths 境界をまたぐ symlink の双方向（許可内から外を指すリンク、許可外から内を指すリンク）が allowed バイパスを通らず削除されないこと、`.` / `..` operand の拒否（非 Git ディレクトリで `-r .` がカレントディレクトリを消さないこと、Git repo の孫ディレクトリからの `-r ..` が親を消さないこと、`-rf .` でも `-f` がバイパスにならないこと、allowed_paths 配下でも `-r sub/..` で許可ディレクトリ全体を消せないこと、`.hidden` / `...` のような dot 始まりの通常ファイルは削除できること、ディレクトリ名を明示した `-r sub` は従来どおり削除できること）も含めて検証

- **追加の fail-closed 回帰テスト**: 設定パスを決定できない `Config::load_from_path(None)` が strict モードへ倒れること、workdir の canonicalize 不能を `IoError` として伝播すること、存在しないパスと読み取り不能な中間ディレクトリ配下の `symlink_metadata()` エラーを `is_real_directory()` が握りつぶさないことを検証する

### 既知の限界

- **TOCTOU**: 事前の Git 管理メタデータ判定と `fs::remove_dir_all` による実削除の間にはレースウィンドウがある。安全性は AI エージェントが単一プロセスで完結する前提を想定しており、別プロセスが削除対象配下に `.git` や bare リポジトリを置く・rename するシナリオは想定外。同一サーバ上で複数の非協力プロセスが同時に同じパスを操作する環境では追加の防御が必要

### バージョン体系

YY.M.NNN 形式（例: 26.2.100）。リリースは GitHub Actions の workflow_dispatch で実行。

## Dependencies

| Crate | 用途 |
|---|---|
| `clap` | CLI引数パース (derive) |
| `path-clean` | パス正規化 |
| `git2` | Git操作 (vendored) |
| `serde` + `toml` | 設定ファイルパース |
| `dirs` | ホームディレクトリ検出 |

### Dev Dependencies

| Crate | 用途 |
|---|---|
| `assert_cmd` | CLIテストヘルパー |
| `predicates` | テストアサーション |
| `tempfile` | テスト用一時ディレクトリ |
